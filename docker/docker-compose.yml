version: "3.8"
services:
  app:
    build:
      context: .
    ports:
      - "5000:5000" # Dash працює на порту 8050
    volumes:
      - .:/app
    environment:
      - ENV=development
      - PYTHONPATH=/app:/app/src
    command: poetry run python src/evaluation.py
